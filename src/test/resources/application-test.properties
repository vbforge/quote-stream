# src/test/resources/application-test.properties
# Used during CI — values injected as env vars by GitHub Actions

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/quotestream_test}
spring.datasource.username=${DB_USER:quotestream}
spring.datasource.password=${DB_PASSWORD:quotestream_test}

spring.jpa.show-sql=false
spring.flyway.enabled=true
logging.level.com.quotestream=WARN
